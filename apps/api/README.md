# STITCH Studio API

Standalone Express API backend for STITCH Studio.

## Commands

```bash
npm install
npm run dev
npm start
npm run migrate:images
```

## Environment

Copy `.env.example` to `.env` and configure MongoDB, auth, and Cloudinary credentials before starting the server.
